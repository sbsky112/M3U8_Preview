# 外部 PostgreSQL 数据库连接
# 请替换为您的实际数据库连接信息

# 如果 PostgreSQL 运行在主机上（使用主机网络模式），使用以下格式：
# DATABASE_URL="postgresql://video:jZixWkjHLW2cLwnN@localhost:5432/video?schema=public"

# 如果 PostgreSQL 运行在其他服务器上，使用实际 IP 或域名：
# DATABASE_URL="postgresql://video:jZixWkjHLW2cLwnN@server-ip:5432/video?schema=public"

# 示例配置（请根据实际情况修改）：
# 注意：当前配置使用主机网络模式，所以使用 localhost 连接主机上的数据库
# 这样可以避免权限问题，因为容器直接使用主机的网络栈
DATABASE_URL="postgresql://video:jZixWkjHLW2cLwnN@localhost:5432/video?schema=public"

# 应用端口配置
# 设置应用在主机上监听的端口（默认为 3000）
APP_PORT=3000

# NextAuth 配置
# NEXTAUTH_URL 会自动根据 APP_PORT 设置，无需手动修改
NEXTAUTH_URL="http://localhost:3000"

# 使用以下命令生成密钥
# Windows: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Mac/Linux: openssl rand -base64 32
NEXTAUTH_SECRET=your-generated-secret-here
#NEXTAUTH_SECRET="9Ci6fE0a8d6cpZHovOJ3xZzyzuPrmn2QMflQPcjxl4I="